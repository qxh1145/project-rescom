{
  "name": "project-rescom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspace backend",
    "typecheck": "npm run typecheck --workspace @rescom/schemas && npm run typecheck --workspace backend",
    "test": "npm test --workspace backend",
    "verify": "npm run build && npm run typecheck && npm test"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "resolve-cwd": "^3.0.0"
  }
}
