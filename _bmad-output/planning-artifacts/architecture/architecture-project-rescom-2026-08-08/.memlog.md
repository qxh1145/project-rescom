---
scope: System Architecture for RESCOM (Launch/Production)
purpose: Define invariants for the platform, ensuring correct bounded contexts, data ownership, and tech stack convergence
altitude: System
updated: 2026-08-16T19:52
---

- (decision) Path: Coaching. Audience: New developers (requires a Solution Design output in addition to the Spine).
- (decision) Monorepo management: Turborepo for sharing packages (e.g., form-schema) between Vercel frontend and Dockerized backend.
- (decision) Background jobs: Run in the same Express process as the API (e.g., node-cron) for simplicity during GO LIVE. Not offloaded to a separate worker container.
- (decision) Caching: Use Redis from day 1 for Time Barrier and rate limiting instead of in-memory caching, ensuring horizontal scalability later.
- (event) spine finalized
- (decision) Backend Stack: Changed from Express to NestJS. Retaining Modular Monolith architecture, leveraging NestJS's native module system.
- (decision) Design Paradigm: Adopt Clean Architecture. Backend modules will follow strict layer separation (Domain, Application, Infrastructure, Presentation) obeying the Dependency Rule.
- (event) Architecture update started from finalized PRD and approved Research Integrity Engine addendum dated 2026-08-16.
- (constraint) Full Research Integrity Engine applies only to Internal Forms; External responses are NOT_ASSESSED and keep existing verification controls.
- (constraint) Response Integrity, Respondent Reliability, and Survey Quality are independent dimensions; new Respondents begin UNESTABLISHED and missing evidence cannot lower scores directly.
- (constraint) All existing RESCOM features remain in scope under Direct Adjustment.
- (decision) AD-8 binds survey execution and integrity processing: only Internal Forms enter the full engine, preventing incompatible assessment claims for External Forms.
- (decision) AD-9 binds Internal Form clients and event storage: integrity telemetry is consent-aware, append-only, server-deduplicated, and non-blocking for response durability.
- (decision) AD-10 binds response persistence and assessment processing: response plus assessment-request outbox are committed atomically, preventing lost or duplicate assessment work.
- (decision) AD-11 binds all assessment producers: scoring is deterministic, framework-independent, policy-versioned, replayable, and append-only by revision.
- (decision) AD-12 binds scoring, reliability, and Survey Quality: dimensions and confidence are stored independently, preventing one quality dimension from silently contaminating another.
- (decision) AD-13 binds security and quality workflows: FraudLog remains separate from integrity assessments, preventing low-quality evidence from becoming an unsupported fraud accusation.
- (decision) AD-14 binds policy deployment and rewards: policies progress SHADOW to ADVISORY to ENFORCED; only separately authorized ENFORCED review may hold rewards.
- (decision) AD-15 binds TrustGraph consumers: PostgreSQL authoritative relations plus rebuildable derived edges are used first, preventing premature graph-database dependency and duplicate ownership.
- (decision) Runtime versions are owned by package manifests and deployment images; the spine names technology families and only records observed scaffold versions in the seed.
- (constraint) Observed code reality on 2026-08-16: frontend Next.js 16.3.0 and React 19.2.8; Prisma client 6.0.0; no backend framework installed; local PostgreSQL image 15-alpine.
- (question) PRD still names Express and in-memory/deferred Redis while architecture adopts NestJS and Redis from Day 1; upstream technical notes must be aligned before Implementation Readiness.
- (decision) AD-16 binds all backend modules: each module exclusively owns writes to its tables; cross-module mutations use application ports or events, and only Ledger writes ledger records.
- (decision) AD-17 binds environments and in-process jobs: dev staging and production use isolated state and secrets; every scheduled or outbox task uses distributed claiming or locking and is retry-safe under multiple API replicas.
- (decision) AD-18 binds integrity APIs and analytics: raw events, restricted signals, and linkage evidence stay inside Integrity/Admin boundaries; Publisher and Respondent consumers use safe projections only.
- (constraint) Prisma package family is inconsistent in the current scaffold: prisma and client 6.0.0 versus @prisma/config ^7.9.1; align before migration or generation work.
- (event) Architecture reviewer gate completed; lint clean and AD-16 through AD-18 selected to close semantic boundary gaps.
- (question) Retention, policy promotion, Survey Quality minimum evidence, and review ownership are deferred with explicit PRD launch gates; architecture may proceed through SHADOW design.
- (event) spine finalized
- (event) Architecture Update resumed on 2026-08-16 from the existing finalized memlog to converge canonical documents with repository reality; Prisma schema and application code remain out of scope.
- (constraint) Authority order is SPEC, final PRD plus approved Addendum, Architecture Spine, Solution Design, epics, then code reality as brownfield evidence and structural seed. Architecture V2 copies are historical proposals and cannot override this chain.
- (decision) Architecture V2 status: both copies are superseded and non-canonical; preserve them with a warning, remove their conflicting influence from active guidance, and defer physical archive or deduplication to a separate repository-cleanup change.
- (decision) AD-1 amended. Binds Economy and every Point mutation. Prevents unbalanced mint or loss, mutable history, and duplicate commands. Rule uses LedgerAccount, LedgerJournal, and two or more append-only LedgerEntry rows per journal and unit whose signed sum is zero; corrections use reversal journals; only Economy writes and balances are derived projections.
- (decision) AD-2 amended. Binds shared Form and API contracts. Prevents incompatible frontend and backend payloads without requiring Turborepo. Rule uses versioned Zod packages in one npm workspace with compatibility tests; exact build tooling is manifest-owned and Turbo is deferred until measured orchestration value.
- (decision) AD-5 and AD-17 amended. Binds background work and deployment topology. Prevents conflicting in-process and separate-service designs. Rule uses one NestJS codebase and image with API and worker entrypoints in production; explicit local colocation is allowed; PostgreSQL owns durable work claims; this remains a modular monolith, not microservices.
- (decision) AD-6 amended. Binds cache, rate limiting, and ephemeral coordination. Prevents Redis becoming a second durable authority or a vertical-slice prerequisite. PostgreSQL remains authoritative for Attempts, session revocation, Outbox, Ledger, and workflows; Redis is added only for multi-replica or measured distributed-state needs with explicit fallback or fail-closed behavior.
- (decision) AD-7 amended. Binds backend dependency direction. Prevents framework coupling at invariant and external seams without imposing four folders on every CRUD feature. Ports and adapters are mandatory for Ledger, Form publication, Integrity scoring, auth/session, storage, AI, email, and cross-module commands or events; simple CRUD may remain feature-local under module ownership.
- (decision) AD-10 amended. Binds Response submission and all durable asynchronous effects. Prevents lost work and duplicate assessments or Ledger journals. Response and OutboxEvent commit together; events have unique identity, aggregate identity and version; PostgreSQL claim leases, retry and backoff, terminal state, and per-handler deduplication are mandatory.
- (decision) AD-11 and AD-14 amended. Binds scoring and operational policy. Prevents score evidence from becoming a hidden business action. Immutable assessment revisions store score, confidence, coverage, reasons, and lineage; a separate immutable IntegrityDecision records operational policy outcome and mode; every policy starts SHADOW and Economy consumes one idempotent command.
- (decision) AD-15 amended. Binds future TrustEdge use. Prevents premature graph work. Authoritative PostgreSQL foreign keys ship first; TrustEdge is a restricted rebuildable projection deferred until stable source relations and a measured query or integrity-evidence need exist.
- (decision) AD-16 amended. Binds all database writes and migrations. Prevents cross-context writes. Identity, Research, Participation, Economy, Integrity, Marketplace, Moderation, Notifications, and AI Adapter have one authoritative aggregate and table ownership map; Integrity components are internal, and cross-context changes use owned ports or versioned commands or events.
- (decision) AD-19 added. Binds Research and Participation identity. Prevents Form and FormVersion ambiguity. Form is the logical survey aggregate; FormVersion is a separate entity unique by formId and versionNumber; published versions are immutable; each Attempt pins one version; Response derives participant and version context from Attempt; authenticated one-completion is enforced at logical Form level and guest participation is explicit.
- (decision) AD-20 added. Binds authentication, sessions, cookies, CORS, CSRF, and security audit. Prevents stateless-token claims from defeating immediate invalidation. Short-lived HttpOnly access JWTs carry a PostgreSQL-authoritative session ID and generation; refresh secrets rotate and are stored hashed; Redis may cache only; logout, password change, new login, and account lock revoke sessions.
- (decision) AD-21 added. Binds production Integrity telemetry and governance. Prevents collection before purpose, consent, minimization, retention, rights, access audit, policy promotion, appeal ownership, and Survey Quality evidence gates are approved. Telemetry remains disabled in production until all gates pass; architecture does not claim legal compliance.
- (decision) AD-22 added. Binds file upload and object storage. Prevents unvalidated files, public object keys, and cross-environment leakage. Storage uses environment-isolated private buckets, upload initiation and finalization, allowlisted type and size, server verification and quarantine or scanning policy, short-lived signed access, lifecycle deletion, and an adapter owned outside domain logic.
- (decision) Structural seed ratifies apps/backend and apps/frontend/my-app, one initial Next.js application, packages/schemas plus packages/shared and packages/types, npm workspaces first, one NestJS backend codebase, and no bulk rename. The current root is not yet a workspace and this remains a target until implemented.
- (version) Runtime baseline selected by user direction and verified against official sources: Node.js 22 LTS. Current repository observations remain Next.js 16.3.0, React 19.2.8, PostgreSQL 15-alpine, Prisma CLI and client 6.0.0 with incompatible config 7.9.1, and no NestJS scaffold. Exact package versions belong in manifests and lockfiles.
- (question) Open production gates requiring named human approval remain: telemetry retention and rights policy; policy-promotion owner and calibration evidence; Integrity review and appeal owner; Survey Quality evidence threshold; RPO, RTO, backup retention and restore cadence; production providers, regions, processor inventory, and legal review.
- (event) Architecture convergence decisions recorded; Spine and companion documents pending redistillation and Reviewer Gate.
- (event) Upstream traceability aligned: PRD NFR-4/NFR-6, FormVersion immutability, FraudLog boundary, Addendum reconciliation, and directly conflicting epic acceptance criteria now conform to the updated Spine; historical readiness output marked superseded.
- (decision) AD-11 and AD-14 clarification propagated upstream: ResponseIntegrityAssessment contains evidence and lineage only; a separately persisted linked IntegrityDecision contains operational outcome and rollout mode, preventing score storage from becoming the business action.
- (decision) AD-16 ownership clarification: Platform Infrastructure owns OutboxEvent claim lease retry dead-letter state and ProcessedHandler deduplication records, while each producer context owns event payload semantics and each consumer owns its business side effect.
- (decision) AD-20 OAuth identity rule: AuthIdentity keys providers by provider subject. A Google email match never auto-links by email alone; an existing password user must authenticate and explicitly link with recent authentication, no duplicate User is created, link changes are audited, and the final login method cannot be removed.
- (decision by architect) Reviewer fix: AD-1 now forbids negative user Available/Pending/Frozen/Escrow/Integrity-Hold balances, orders balance-row locks, posts projection and entries atomically, and requires exact unique reversal journals; zero-sum alone was insufficient against concurrent double-spend.
- (decision by architect) Reviewer fix: Internal Form start atomically creates Attempt plus one IN_PROGRESS Response identity; submission transitions it and emits independent InternalRewardRequested and IntegrityAssessmentRequested events so SHADOW/ADVISORY rewards never depend on scoring availability.
- (decision by architect) Reviewer fix: AD-10 now requires fenced PostgreSQL claims, owner/token-matched completion, per-handler monotonic aggregate versions with gap deferral, and atomic database-local domain effect plus processed-handler record.
- (decision by architect) External Completion Code is scoped to one published External FormVersion, disclosed once and stored as a keyed verifier; rotation creates a new version and validation binds active Attempt, exact version, server time, and durable abuse state.
- (constraint by architect) Qualified Privacy/Legal review gates all production personal-data processing; production Integrity telemetry has the additional consent, field-allowlist, policy-owner, review-owner, and evidence-threshold gate. No compliance claim is made.
- (decision by architect) Final PRD deployment targets are Vercel frontend and Cloudflare before Docker/VPS backend; exact managed PostgreSQL choice, storage/email providers, and regions remain open. Next.js uses one app with route groups that do not substitute for server authorization.
- (event by architect) Applied first Reviewer Gate load-bearing fixes across Spine, Solution Design, PRD, epics, and reconciliation; preparing deterministic lint and reviewer re-audit.
- (decision by architect) Reviewer precision fix: reversesJournalId is unique per original journal; each journal reverses at most once and reversal-of-reversal forms a non-branching chain.
- (decision by architect) Reviewer precision fix: ordering-sensitive Outbox handlers consume a complete named orderingStream with contiguous streamSequence; aggregateVersion remains domain concurrency metadata, not a partial-subscription offset.
- (decision by architect) Named financial workflow coordinators now bind publication+Escrow, External completion+Pending, Internal reward, ENFORCED hold, External dispute, close+refund, and top-up to either one shared PostgreSQL Unit of Work or source+Outbox with stable Economy command IDs and explicit failure recovery.
- (decision by architect) ENFORCED assessment terminal failure or governance deadline emits an idempotent fail-open hold release plus incident; Guest Internal submission never emits an account reward event.
- (decision by architect) CSRF bootstrap accepts valid access or valid refresh cookie plus exact origin/Fetch-Metadata; refresh presents current CSRF and atomically rotates refresh secret and CSRF hash, preventing expired-access reload deadlock.
- (decision by architect) Deployment declares REDIS_DISABLED_SINGLE_REPLICA or REDIS_SHARED. The first uses PostgreSQL durable abuse state and rejects multi-replica startup; configured Redis outage fails high-risk mutations closed.
- (decision by architect) Completion verifier stores keyVersion plus keyed digest bound to FormVersion; active keys remain available, three failures lock Attempt, account+FormVersion limits apply, and IP/device scopes require Privacy approval.
- (decision by architect) Outbox dead-letter remains a blocking stream gap until audited re-drive/skip; critical skips require compensation/fail-safe. External adapters persist attempts, reuse idempotency keys, reconcile status, and reject irreversible providers without safe retry support.
- (decision by architect) A Platform Migration Integrator owns repository-wide migration order, cross-context FK review and apply-once gate; referencing-table context authors the FK while every context retains table ownership.
- (decision by architect) Platform Infrastructure owns technical StoredObject metadata and quarantine state machine; domain context owns attachment relation, scan outage fails closed, and only CLEAN objects attach/download.
- (decision by architect) Scheduled financial/security jobs inherit AD-10 fencing, availability, retry/backoff, dead-letter, audited disposition, and idempotent-command semantics; External Attempt start mirrors Internal eligibility/quota/reservation/server-time checks.
- (question by architect) Open gate: Product/Research/Moderation and Engineering must approve the Form publication/moderation transition table before schema/client generation.
- (question by architect) Open gate: Product, Security, and Privacy must approve Guest Internal abuse scopes, bot challenge, reservation expiry, retention, monitoring, and support posture before shared/production enablement.
- (constraint by architect) Completion Code verification is constant-time and plaintext/input codes are never logged; contract tests must cover these properties.
- (event by reviewer-gate) Final Reviewer Gate: rubric PASS with 0 Critical/0 High (PROJECT_SUMMARY stale remains out-of-scope advisory); adversarial PASS with 0 findings; version-reality PASS with 0 Critical/0 High and only implementation advisories.
- (event by architect) Architecture Update finalized: deterministic spine lint 0 findings, git diff check passed, canonical documents converged, and application/schema/package/migration files were not changed by this run.
